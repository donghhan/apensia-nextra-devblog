# Django와 Gulp의 만남

## 디버깅

### 연산자 ParseError

gulp-htmlmin 패키지를 이용해 HTML 파일을 축소화하는 과정에서 다음과 같은 문제가 발생하였다.

```
Message:
    Parse Error: <= 3 %}
                <span class="page_btn current">{{ page_obj.number }}</span>
            {% endif %}
        </div>
    {% endif %}
</section>
Details:
    fileName: /Users/apensia/dev/knower-admin/client/templates/components/paginator.html
    domainEmitter: [object Object]
    domainThrown: false
```
