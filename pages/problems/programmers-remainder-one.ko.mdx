---
title: 프로그래머스 - 나머지가 1이 되는 수 구하기 
image: https://www.tabnine.com/academy/wp-content/uploads/2020/10/academy_1.png
description: '프로그래머스 나머지가 1이 되는 수 구하기'
date: September 11th, 2022
---

import Callout from 'nextra-theme-docs/callout'
import Bleed from 'nextra-theme-docs/bleed'
import { SiJavascript, SiPython } from 'react-icons/si'

# 나머지가 1이 되는 수 구하기

## 문제

자연수 `n`이 매개변수로 주어집니다. `n`을 `x`로 나눈 나머지가 1이 되도록 하는 가장 작은 자연수 `x`를 `return` 하도록 `solution` 함수를 완성해주세요. 답이 항상 존재함은 증명될 수 있습니다.

## 제한 사항

3 ≤ `n` ≤ 1,000,000

## 입출력 예

<br/>

<center>
| 입력 | 출력 |
| ---- | ---- |
| 10   | 3    |
| 12   | 11   |
</center>

1. 10을 3으로 나눈 나머지가 1이고, 3보다 작은 자연수 중에서 문제의 조건을 만족하는 수가 없으므로, 3을 return 해야 합니다.
2. 12를 11로 나눈 나머지가 1이고, 11보다 작은 자연수 중에서 문제의 조건을 만족하는 수가 없으므로, 11을 return 해야 합니다.

## 풀이

가장 원초적인 방법으로 자연수 1을 제외한 2부터 `n` 전까지 모든 수로 나눠봤을 때 나머지가 1이 되는 모든 수를 배열에 집어넣는다.
모든 수는 가장 작은 수부터 넣어지므로 배열의 맨 첫번째 인덱스를 `return`해주면 된다.

<br/>

### 파이썬

```py
answer = []

for i in range(n):
    if n % (i + 1) == 1:
        answer.append(i + 1)

return answer[0]
```

<br/>

### 자바스크립트

```js
const arr = [];

for (let i = 2; i < n; i++) {
    if (n % i === 1) {
        arr.push(i);
    }
}

return arr[0];
```

## 다른 사람 풀이

<details>
  <summary>펼쳐보기</summary>
  <p>
    `while`문을 이용하여 `n`을 어떤 수로 나눴을 때 1로 떨어지지 않을 때까지 `answer`에 1을 더한다. 
    어차피 가장 작은 수만 반환하면 그만이기에 `while`문 안에 있는 조건이 만족되면 자연스럽게 반복문은 종료되고
    우리가 원하는 수를 반환해준다.

    <h3>파이썬</h3>

    ```py
    def solution(n):
        return [x for x in range(1,n+1) if n%x==1][0]
    ```

    <h3>자바스크립트</h3>

    ```js
    const answer = 1;
    while (n % answer !== 1) answer++;
    return answer;
    ```

  </p>
</details>
