---
title: Gulp configuration for Pug, SCSS and TypeScript
image: https://course.oc-static.com/courses/7107341/7107341_teaser_picture_1633008310.jpg
description: 'In this post, we are going to go over how to use Gulp for making static websites.'
date: June 21st, 2022
---

import Callout from 'nextra-theme-docs/callout'
import Bleed from 'nextra-theme-docs/bleed'

# Playing with Gulp

## What is Gulp?

<a href="https://gulpjs.com/" target="_blank">Gulp</a> is an automated task runner based on NodeJS and NPM.
In this context, what do we mean by "tasks" indicate a series of JavaScript functions that are executed by the time they are called such as compiling SASS/SCSS into CSS, compiling TypeScript into JavaScript and so on.

### Difference between Webpack and Gulp

This will lead us to the question: Then what is the difference between Webpack and Gulp?

I created the table that shows the difference between Webpack and Gulp.[^1]

|                          |                                 Webpack                                 |                                        Gulp                                         |
| :----------------------: | :---------------------------------------------------------------------: | :---------------------------------------------------------------------------------: |
|        Definition        | Module Bundler that combines different files into one consolidated file | Automated task runner that carries out the different types of tasks simultaneously. |
| Configuration Difficulty |                                  Hard                                   |                                        Easy                                         |
|      Code required       |                                  More                                   |                                        Less                                         |
|     Appropriate for      |               Application with multiple complex processes               |               Simple application that does not take too much memories               |

[^1]: Source: <a href="https://www.educba.com/gulp-vs-webpack/" target="_blank">Difference between Webpack and Gulp</a>

<hr/>

## Setup for Gulp

Let's go over each section one by one.

### Installing Gulp

We will installing the <a href="https://gulpjs.com/docs/en/getting-started/quick-start#install-the-gulp-command-line-utility" target="_blank">gulp-cli</a> first.

```bash
npm i -g gulp-cli
```

<br/>

Next, inside the project folder, we will create `package.json` file with the following command:

```bash
npm init -y
```

This command will allow you to create `package.json` file automatically <a href="https://docs.npmjs.com/cli/v8/commands/npm-init#yes" target="_blank">with answering all questions "yes"</a>.

<br/>

Then, we will <a href="https://gulpjs.com/docs/en/getting-started/quick-start#install-the-gulp-package-in-your-devdependencies" target="_blank">install Gulp</a>.

```bash
# Using NPM
npm i -D gulp

# Using yarn
yarn add -D gulp
```

<br/>

### Creating `gulpfile.js`

<a href="https://gulpjs.com/docs/en/getting-started/javascript-and-gulpfiles#gulpfile-explained" target="_blank"><code>gulpfile.js</code></a> is a Gulp's configuration file where we can define all tasks that we desire.

```bash
# For MAC/Linux users
touch gulpfile.js

# For Windows users
echo.> gulpfile.js
```

#### Using with Babel (Optional)

In order to use `gulpfile.js` with JavaScript ES6 version, we would need to use <a href="https://babeljs.io/" target="_blank">Babel</a>, which converts modern version JavaScript into an older version so that old browser could also understand the code.

To do so, we will firstly rename `gulpfile.js` into `gulpfile.babel.js`.

Next, we will install additional packages.

```bash
# Using NPM
npm i -D @babel/core @babel/preset-env

# Using yarn
yarn add -D @babel/core @babel/preset-env
```

Then, we will create `babel.config.json` in the project root and include the following code.
By doing this, we can set up the `@babel/preset-env` and use it for our project.

```json babel.config.json
{
    "presets": ["@babel/preset-env"]
}
```

So far, our folder structure will look as below:

<br/>

<center>![folder_structure](../../media/gulp-configuration/gulp_folder_structure.png)</center>

<br/>

Inside of our `package.json` file, we will include two `scripts` commands:

1. `dev`: This is for development
2. `build`: This is for build

```json package.json
"scripts": {
    "dev": "gulp dev",
    "build": "gulp build"
}
```

<br/>

We will put all files that are to be compiled within `src/` folder.

```bash
mkdir src/
```

<hr/>

## Converting Pug into HTML

<a href="https://pugjs.org/api/getting-started.html" target="_blank">Pug</a> is a template engine for NodeJS. We will be converting Pug into HTML files.

For converting Pug files into normal HTML files, we will be using <a href="https://github.com/gulp-community/gulp-pug#readme" target="_blank">gulp-pug</a> package.

```bash
# NPM
npm i -D gulp-pug

# yarn
yarn add -D gulp-pug
```

<br/>

<CH.Scrollycoding>

### Importing

Let's import some necessary items from packges.
If you are using Babel, you can use `import` keyword.

```js gulpfile.js focus=1:6
const { src, dest } = require('gulp');
const pug = require('gulp-pug');

// If you are using Babel with Gulp
// import { src, dest } from 'gulp';
// import pug from 'gulp-pug';

const routes = {
    pug: {
        src: "src/**/*.pug",
        build: "build"
    }
}

function PugIntoHTML() {
    return;
}
```

---

### Routes

I created object called `routes`, which specifies the routes for each files that are in development and files that will be sent to build folder.

```js gulpfile.js focus=8:13
const { src, dest } = require('gulp');
const pug = require('gulp-pug');

// If you are using Babel with Gulp
// import { src, dest } from 'gulp';
// import pug from 'gulp-pug';

const routes = {
    pug: {
        src: "src/**/*.pug",
        build: "build"
    }
}

function PugIntoHTML() {
    return;
}
```

---

### Defining task

Let's create a task for converting Pug files into HTML files.
I named it as `PugIntoHTML()`.

> Beginning from Gulp version, it is recommended to make task as a function instead of using `gulp.task`.

1. First, I need to let `PugIntoHTML()` task look for the files that are to be converted in `src/` file with using <a href="https://gulpjs.com/docs/en/api/src" target="_blank"><code>src()</code></a> method.
2. With using `pipe()`, I connected to the next task with `pug()` method from `gulp-pug` package.
3. Lastly, I designated routes for build files with using <a href="https://gulpjs.com/docs/en/api/dest" target="_blank"><code>dest()</code></a>.

```js gulpfile.js focus=15:18
const { src, dest } = require('gulp');
const pug = require('gulp-pug');

// If you are using Babel with Gulp
// import { src, dest } from 'gulp';
// import pug from 'gulp-pug';

const routes = {
    pug: {
        src: "src/**/*.pug",
        build: "build"
    }
}

function PugIntoHTML() {
    return src(routes.pug.src)
        .pipe(pug())
        .pipe(dest(routes.pug.build));
}
```

---

### Registering tasks.

Lastly, I used <a href="https://gulpjs.com/docs/en/api/series" target="_blank"><code>series()</code></a> method for connecting our task into our script command.

```js gulpfile.js focus=1,21
const { src, dest, series } = require('gulp');
const pug = require('gulp-pug');

// If you are using Babel with Gulp
// import { src, dest } from 'gulp';
// import pug from 'gulp-pug';

const routes = {
    pug: {
        src: "src/**/*.pug",
        build: "build"
    }
}

function PugIntoHTML() {
    return src(routes.pug.src)
        .pipe(pug())
        .pipe(dest(routes.pug.build));
}

export const dev = series([PugIntoHTML]);
```

</CH.Scrollycoding>

### Result

Now that we are done with configuraiton, let's make `index.pug` file within `src/` folder and hit `npm run dev` or `yarn dev`.

```pug src/index.pug
doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
```

After running `npm run build` or `yarn build`, our `index.pug` file is converted into `index.html` with simplified code.

<center>![html_converted](../../media/gulp-configuration/gulp_pug_html_compiling.png)</center>

<br/>

## Converting SCSS into CSS

Let's install below packages.

```bash
# npm
npm i -D gulp-sass sass gulp-autoprefixer gulp-uglifycss

# yarn
yarn add -D gulp-sass sass gulp-autoprefixer gulp-uglifycss
```

1. <a href="https://github.com/dlmanning/gulp-sass#readme" target="_blank">gulp-sass</a>: SASS plugin for Gulp. With this plugin, Gulp will convert SCSS/SASS into CSS with Dart package of <a href="https://github.com/sass/dart-sass" target="_blank">sass</a>.
2. <a href="https://github.com/sindresorhus/gulp-autoprefixer#readme" target="_blank">gulp-autoprefixer</a>: Gulp will convert code our SCSS/SASS code into compatible CSS code that older browsers can understand.
3. <a href="https://github.com/ubirak/gulp-uglifycss" target="_blank">gulp-uglifycss</a>: Make transfiled CSS files shortened to increase performance.

<br/>
<br/>

<CH.Scrollycoding>

### Setting routes

After importing packages, I defined the routes for SCSS files and CSS files that will be placed to in the `routes` object,

```js gulpfile.js focus=3:5,10:13
const { src, dest, series } = require('gulp');
const pug = require('gulp-pug');
const sass = require('gulp-sass')(requrie('sass'));
const autoPrefixer = require("gulp-autoprefixer");
const uglify = require("gulp-uglify");

const routes = {
    pug: {
        src: "src/**/*.pug",
        build: "build"
    },
    sass: {
        src: "src/scss/style.scss",
        build: "build/css/style.css"
    }
}

function PugIntoHTML() {
    return src(routes.pug.src)
        .pipe(pug())
        .pipe(autoPrefixer())
        .pipe(dest(routes.pug.build));
}

function SCSStoCSS() {
    return;
}

export const dev = series([PugIntoHTML, SCSStoCSS]);
```

---

### Defining task

I created task called `SCSStoCSS()` for converting SCSS files to CSS files.

```js gulpfile.js focus=24:26
const { src, dest, series } = require('gulp');
const pug = require('gulp-pug');
const sass = require('gulp-sass')(requrie('sass'));
const autoPrefixer = require("gulp-autoprefixer");
const uglify = require("gulp-uglify");

const routes = {
    pug: {
        src: "src/**/*.pug",
        build: "build"
    },
    sass: {
        src: "src/scss/style.scss",
        build: "build/css/style.css"
    }
}

function PugIntoHTML() {
    return src(routes.pug.src)
        .pipe(pug())
        .pipe(dest(routes.pug.build));
}

function SCSStoCSS() {
    return;
}

export const dev = series([PugIntoHTML, SCSStoCSS]);
```

---

#### Converting SCSS into CSS

We will convert SCSS files located inside of `src/` folder.
I exactly followed what it showed on the <a href="https://github.com/dlmanning/gulp-sass#render-your-css" target="_blank">documentation</a>.

```js gulpfile.js focus=26
const { src, dest, series } = require('gulp');
const pug = require('gulp-pug');
const sass = require('gulp-sass')(requrie('sass'));
const autoPrefixer = require("gulp-autoprefixer");
const uglify = require("gulp-uglify");

const routes = {
    pug: {
        src: "src/**/*.pug",
        build: "build"
    },
    sass: {
        src: "src/scss/style.scss",
        build: "build/css/style.css"
    }
}

function PugIntoHTML() {
    return src(routes.pug.src)
        .pipe(pug())
        .pipe(dest(routes.pug.build));
}

function SCSStoCSS() {
    return src(routes.scss.src)
        .pipe(sass().on("error", sass.logError))
        .pipe(autoPrefixer())
        .pipe(
            uglifycss({
                maxLineLen: 80,
                uglyComments: true,
            })
        )
        .pipe(dest(routes.scss.build));
}

export const dev = series([PugIntoHTML, SCSStoCSS]);
```

---

#### Connecting auto prefixer

Next, I connected the <a href="https://github.com/sindresorhus/gulp-autoprefixer#readme" target="_blank">gulp-autoprefixer</a> shown on the <a href="https://github.com/sindresorhus/gulp-autoprefixer#usage" target="_blank">documentation</a>.
If you want, you are more than welcomed to include options <a href="https://github.com/postcss/autoprefixer#options" target="_blank">available here</a>.

```js gulpfile.js focus=27
const { src, dest, series } = require('gulp');
const pug = require('gulp-pug');
const sass = require('gulp-sass')(requrie('sass'));
const autoPrefixer = require("gulp-autoprefixer");
const uglify = require("gulp-uglify");

const routes = {
    pug: {
        src: "src/**/*.pug",
        build: "build"
    },
    sass: {
        src: "src/scss/style.scss",
        build: "build/css/style.css"
    }
}

function PugIntoHTML() {
    return src(routes.pug.src)
        .pipe(pug())
        .pipe(dest(routes.pug.build));
}

function SCSStoCSS() {
    return src(routes.scss.src)
        .pipe(sass().on("error", sass.logError))
        .pipe(autoPrefixer())
        .pipe(
            uglifycss({
                maxLineLen: 80,
                uglyComments: true,
            })
        )
        .pipe(dest(routes.scss.build));
}

export const dev = series([PugIntoHTML, SCSStoCSS]);
```

---

#### Uglifying CSS files

Finally, I configured the uglifying CSS based on <a href="https://github.com/ubirak/gulp-uglifycss#usage" target="_blank">documentation</a>.

```js gulpfile.js focus=26:31
const { src, dest, series } = require('gulp');
const pug = require('gulp-pug');
const sass = require('gulp-sass')(requrie('sass'));

const routes = {
    pug: {
        src: "src/**/*.pug",
        build: "build"
    },
    sass: {
        src: "src/scss/style.scss",
        build: "build/css/style.css"
    }
}

function PugIntoHTML() {
    return src(routes.pug.src)
        .pipe(pug())
        .pipe(dest(routes.pug.build));
}

function SCSStoCSS() {
    return src(routes.scss.src)
        .pipe(sass().on("error", sass.logError))
        .pipe(autoPrefixer())
        .pipe(
            uglifycss({
                maxLineLen: 80,
                uglyComments: true,
            })
        )
        .pipe(dest(routes.scss.build));
}

export const dev = series([PugIntoHTML, SCSStoCSS]);
```

---

#### Connecting tasks

Finally, I connected tasks with `dev` script command.

```js gulpfile.js focus=35
const { src, dest, series } = require('gulp');
const pug = require('gulp-pug');
const sass = require('gulp-sass')(requrie('sass'));

const routes = {
    pug: {
        src: "src/**/*.pug",
        build: "build"
    },
    sass: {
        src: "src/scss/style.scss",
        build: "build/css/style.css"
    }
}

function PugIntoHTML() {
    return src(routes.pug.src)
        .pipe(pug())
        .pipe(dest(routes.pug.build));
}

function SCSStoCSS() {
    return src(routes.scss.src)
        .pipe(sass().on("error", sass.logError))
        .pipe(autoPrefixer())
        .pipe(
            uglifycss({
                maxLineLen: 80,
                uglyComments: true,
            })
        )
        .pipe(dest(routes.scss.build));
}

export const dev = series([PugIntoHTML, SCSStoCSS]);
```

</CH.Scrollycoding>

### Result

Our build command will convert SCSS files into CSS in our designated routes.

<CH.Code>

```scss src/scss/style.scss
body {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}
```

```scss src/scss/_variable.scss
$yellow: #e8bf5d;
```

</CH.Code>

<center>![html_converted](../../media/gulp-configuration/scss_to_css.png)</center>

<br/>

## Watching

It is quite annoying if I need to manually hitting refresh button every time changes happen.
For this, I would be using the <a href="https://browsersync.io/#install" target="_blank">browser-sync</a>.

<br/>

<CH.Scrollycoding>

### Defining task

I created task called `Watch()` and let `browserSync` begin a server with <a href="https://browsersync.io/docs/api#api-init" target="_blank"><code>.init()</code></a>, pointing out `build` folder.

```js gulpfile.js focus=4,28:35
const { src, dest, series } = require('gulp');
const pug = require('gulp-pug');
const sass = require('gulp-sass')(requrie('sass'));
const browserSync = require("browser-sync").create();

const routes = {
    pug: {
        src: "src/**/*.pug",
        build: "build"
    },
    sass: {
        src: "src/scss/style.scss",
        build: "build/css/style.css"
    }
}

function PugIntoHTML() {
    return src(routes.pug.src)
        .pipe(pug())
        .pipe(dest(routes.pug.build));
}

function SCSStoCSS() {
    return src(routes.scss.src)
        .pipe(sass().on("error", sass.logError))
        .pipe(dest(routes.scss.build));
}

function Watch() {
  browserSync.init({
    server: {
      baseDir: "./build",
    },
  });
}

export const dev = series([PugIntoHTML, SCSStoCSS]);
```

---

### Making Gulp watch

With using <a href="https://gulpjs.com/docs/en/api/watch" target="_blank"><code>watch()</code></a>

```js gulpfile.js focus=1,35:37
const { src, dest, series, watch } = require('gulp');
const pug = require('gulp-pug');
const sass = require('gulp-sass')(requrie('sass'));
const browserSync = require("browser-sync").create();

const routes = {
    pug: {
        src: "src/**/*.pug",
        build: "build"
    },
    sass: {
        src: "src/scss/style.scss",
        build: "build/css/style.css"
    }
}

function PugIntoHTML() {
    return src(routes.pug.src)
        .pipe(pug())
        .pipe(dest(routes.pug.build));
}

function SCSStoCSS() {
    return src(routes.scss.src)
        .pipe(sass().on("error", sass.logError))
        .pipe(dest(routes.scss.build));
}

function Watch() {
  browserSync.init({
    server: {
      baseDir: "./build",
    },
  });
  watch(routes.scss.src, SCSStoCSS);
  watch(routes.html.src, MinifyHTML);
}

export const dev = series([PugIntoHTML, SCSStoCSS]);
```

</CH.Scrollycoding>
